#credits @themayankparmar (Telegram)

from pyrogram import Client, filters
import asyncio
app=client(session_name=Pyro String, api_id=apiidhere, api_hash=apihashhere)
 
async def main() :
    async with app:
        while true:
            await app.send_message(message.chat.id, " pro ", reply_to_message_id=urmessageid)
            await asyncio.sleep(5)
 
 
app.run(main())
